# Changelog

本项目的主要变更都会记录在此文件中。版本格式遵循
[Semantic Versioning](https://semver.org/)。

## [1.3.0] - 2026-09-24

### Added

- 支持同时订阅多个 ntfy 服务器/主题，每个订阅独立连接、独立自动重连。
- 设置界面新增订阅列表：可添加、删除、启用/停用，并为每个订阅单独配置
  服务器、主题和认证信息。
- 弹出面板会合并展示所有订阅的消息，并标注消息来源（订阅名/主题）。
- 发布消息时可选择发布到哪个订阅（多个订阅时显示下拉选择）。

### Changed

- 消息去重键改为“服务器 + 主题 + 消息 ID”，避免不同服务器同名主题互相覆盖。
- `dms ipc call ntfyCenter status` 现在返回 `subscriptions` 数组及各订阅状态。
- 旧版单服务器/单主题配置在首次打开设置时自动迁移为单个订阅。

## [1.2.0] - 2026-07-24

### Added

- 从中英文验证码短信中提取 4–8 位数字或字母数字验证码。
- 在消息卡片中显示识别出的验证码，点击卡片可直接复制验证码。
- 系统通知发送失败时自动回退到 Freedesktop `notify-send`。

### Changed

- 实时消息和启动时加载的历史消息共用同一套验证码解析逻辑。
- 没有识别出验证码时，点击消息卡片仍然复制完整正文。

## [1.1.0] - 2026-07-24

### Added

- ntfy JSON 流后台订阅与自动重连。
- DMS 桌面通知和 DankBar 消息历史面板。
- 从 DankBar 发布自定义文本。
- Access Token 和 HTTP Basic 认证。
- TLS 证书验证开关。
- 可配置的历史数量、历史时间范围和默认发布标题。
- DMS 原生插件设置界面。
- 随插件发布的零第三方依赖 Python helper。

### Changed

- 将服务器、主题和认证信息迁移到 DMS 插件设置命名空间。
- 去除对外部 `~/.config/ntfy-dms/config.json` 的依赖。
- 去除对外部 `~/.local/bin/ntfy-dms` helper 的依赖。
- 按服务器和主题隔离重连消息游标。

### Fixed

- 修复消息实际发布成功但界面显示发送失败的问题。
- 修复历史加载与实时消息同时发生时可能覆盖新消息的问题。
